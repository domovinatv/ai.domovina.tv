/// Native stub — na iOS/Android/macOS mute ide kroz `player.setVolume`.
library;

import 'package:media_kit/media_kit.dart';

bool setElementMuted(Player player, bool muted) => false;

bool? elementMuted(Player player) => null;
