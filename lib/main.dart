import 'src/app.dart';

/// Ulazna točka DOMOVINA.ai ljuske. Sav kod je u `lib/src/app.dart` i dalje;
/// ova datoteka namjerno ostaje prazna da bi se `lib/` mogao izdvojiti u
/// `podcast_core` paket bez promjene ponašanja.
Future<void> main() => runPodcastApp();
