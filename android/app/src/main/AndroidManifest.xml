<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network access — obavezno eksplicitno navesti za release build
         (debug build ima automatski, release bez toga crasha na DNS lookupu). -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Background audio playback (audio_service plugin) -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <!-- Android TV (Leanback): touchscreen nije obavezan, leanback je preferred.
         Bez ovih featurea Play Store ne dopusta TV listing, a TV launcher ne
         registrira app kao LEANBACK target. Vidi lib/services/tv_mode.dart. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:label="DOMOVINA"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Leanback (Android TV) launcher entry. Bez ovog filtera app
                 ne pojavljuje se u TV "Apps" redu na home screenu. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- Deep Links (App Links) — ALLOWLIST putanja, ne cijeli host.
                 Android nema "exclude" (za razliku od iOS AASA components),
                 pa bi filter bez path atributa oteo i OAuth povratak
                 https://domovina.ai/auth/callback iz Chromea → web prijava
                 bi se prekinula otvaranjem appa (isti bug kao iOS AASA
                 paths:["*"], popravljeno 2026-07-22). Auth rute (/auth/*,
                 /login-callback, /youtube-claim/*, /invite) NAMJERNO nisu
                 ovdje — moraju ostati u browseru. Novu javnu content rutu
                 dodaj i ovdje i u iOS AASA (web/_worker.js AASA_JSON). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="domovina.ai" />
                <data android:path="/" />
                <data android:path="/channels" />
                <data android:pathPrefix="/v/" />
                <data android:pathPrefix="/m/" />
                <data android:pathPrefix="/c/" />
                <data android:pathPrefix="/p/" />
                <data android:pathPrefix="/handoff" />
                <!-- „Izborni dan" — javna ljestvica glasanja (/glasanje) i
                     deep-link na kandidata (/glasanje/<slug>). Par u iOS AASA
                     components je u web/_worker.js AASA_JSON. -->
                <data android:path="/glasanje" />
                <data android:pathPrefix="/glasanje/" />
            </intent-filter>

            <!-- Custom URL Scheme za Supabase OAuth / Magic Links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ai.domovina" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Disable Impeller renderer. On low-end Amlogic GPU (EON SDSTB02)
             Impeller's first-frame shader compile adds 5-10s to cold start.
             Skia fallback has pre-compiled shaders → faster TTID. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- audio_service: foreground media playback service + Bluetooth/headset
             button receiver. Required for background audio. -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: Android 11+ zahtijeva eksplicitnu deklaraciju
             namjera kako bi launchUrl() mogao pronaci browser za http(s). -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
